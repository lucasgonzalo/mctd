services:
  mctd:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # persist models & results on the host, outside the container
      - ./models:/app/models
      - ./results:/app/results
